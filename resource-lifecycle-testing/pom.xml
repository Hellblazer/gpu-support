<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.hellblazer.gpu-support</groupId>
        <artifactId>gpu-support.app</artifactId>
        <version>1.0.7-SNAPSHOT</version>
    </parent>

    <artifactId>resource-lifecycle-testing</artifactId>
    <packaging>jar</packaging>

    <name>Resource Lifecycle Testing</name>
    <description>
        Portable, reusable testing framework for resource lifecycle verification.
        Provides snapshot/diff/assert capabilities for detecting resource leaks
        in components using the ResourceTracker/ResourceHandle pattern.
        Technology-agnostic: works with any resource type (GPU, file, network, etc).
    </description>

    <dependencies>
        <!-- Resource Management Framework (REQUIRED) -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>resource</artifactId>
        </dependency>

        <!-- JUnit 5 Integration (REQUIRED for base class) -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
        </dependency>

        <!-- Test Dependencies -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Surefire Plugin for Testing -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                    </includes>
                </configuration>
            </plugin>

            <!-- JAR Plugin for Distribution -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>lib/</classpathPrefix>
                        </manifest>
                        <manifestEntries>
                            <Implementation-Title>Resource Lifecycle Testing</Implementation-Title>
                            <Implementation-Version>${project.version}</Implementation-Version>
                            <Automatic-Module-Name>com.hellblazer.luciferase.test.lifecycle</Automatic-Module-Name>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
